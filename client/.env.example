# From: node scripts/create-agent.mjs (run from the repo root) -> "Created agent: <id>"
VITE_ELEVENLABS_AGENT_ID=
# Leave as /api when running the backend locally (Vite proxies it to :8787).
VITE_BACKEND_URL=/api
